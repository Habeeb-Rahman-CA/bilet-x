{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Frontend uses only invoke, gated by generate_handler!. No core APIs called from JS, so no core permissions required.",
  "windows": [
    "main"
  ],
  "permissions": []
}
